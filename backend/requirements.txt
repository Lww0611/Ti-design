# -----------------------------------------------------------------------------
# Ti-design backend — Python 依赖
# 安装：在 backend 目录下  python -m pip install -r requirements.txt
# PyTorch：默认从 PyPI 装 CPU 版；若需 CUDA，请按 https://pytorch.org 单独安装对应 torch 后再装其余包。
# Render：在 Dashboard 设 Root Directory=backend 时用 backend/runtime.txt；若 Root 为仓库根目录，用仓库根目录的 runtime.txt。务必为 Python 3.11，或在环境变量设 PYTHON_VERSION=3.11.9。
# torch：3.11 及以下锁 2.5.1（与训练机一致）；3.12+ 无 2.5.1 wheel 时改用 2.9.x。
# -----------------------------------------------------------------------------

# HTTP API
fastapi>=0.115.0,<0.120.0
uvicorn[standard]>=0.30.0,<0.35.0
python-multipart>=0.0.9

# Schemas（FastAPI 会拉 pydantic；此处显式约束主版本）
pydantic>=2.4.0,<3

# Database — 支持 MySQL 与 PostgreSQL（Render）
sqlalchemy>=2.0.0,<3
pymysql>=1.1.0,<2
psycopg[binary]>=3.2.0,<4

# Data & tabular / 经典 ML
numpy>=1.24.0,<3
pandas>=2.0.0,<3
# 注意：曾写死 1.7.1，当前 PyPI 常见最新为 1.6.x；若训练环境与版本强绑定请再对齐
scikit-learn>=1.4.0,<1.7
joblib>=1.3.0
xgboost>=2.0.0,<3

# Auth — passlib CryptContext(schemes=["bcrypt"])
passlib[bcrypt]>=1.7.4

# -----------------------------------------------------------------------------
# BERT-XGB / MatSciBERT — pickle 与 encoder 类路径依赖 transformers/torch 版本
# 训练机已确认时可保持：torch==2.5.1, transformers==4.45.2（仅 Python<3.12）
# sentence-transformers：原 5.2.0 在 PyPI 无对应发布，改为当前可用 5.1.x
# -----------------------------------------------------------------------------
torch==2.5.1; python_version < "3.12"
torch>=2.9.0,<2.12; python_version >= "3.12"
transformers==4.45.2
sentence-transformers>=5.1.0,<5.2.0
