# 本地：可复制为 .env.development
VITE_API_ORIGIN=http://127.0.0.1:8000

# Vercel（Production / Preview）：在 Project Settings → Environment Variables 添加同名变量
# 值为 Render 后端公网地址，勿带末尾斜杠，例如：
# VITE_API_ORIGIN=https://ti-design-api-xxxx.onrender.com
# 保存后必须 Trigger Redeploy，否则仍是打包时的旧值（会变成 127.0.0.1）
